  <div class="container">
		<div class="row centered mt mb">
			<div class="col-lg-8 col-lg-offset-2">
                <h1><%= @title %></h1>
                <p>These are tweets being pulled from twitter by using the twitter API that I coded into this website.</p>
                <br/>
                <%  @search_results.each do |tweet| %>
                    <p><a href=""><%= tweet.user.screen_name %></a> <%= tweet.text %></p>
                <% end %>
            </div>
		</div>
	</div>
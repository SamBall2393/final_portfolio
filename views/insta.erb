  <div class="container">
		<div class="row centered mt mb">
			<div class="col-lg-8 col-lg-offset-2">
                <h1><%= @title %></h1>
                    <p>These are images being pulled from instagram by using the instagram API that I coded into this website.</p>
                <br/>
                    <% @photos.each do |photo| %>
                        <img src="<%= photo.images.thumbnail.url %>" />
                    <% end %>
            </div>
		</div>
	</div>